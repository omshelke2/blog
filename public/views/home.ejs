<%-include('partials/header')%>
<h1>Hello WOrld</h1>

<p><%=homeStartingContent%></p>

<!-- <% for(var i=0;i<posts.length;i++){ %>
    
   
    
    <h2><%= posts[i].title %></h2>
    <p><%= posts[i].blogBody %></p>
<%  } %> -->

<% posts.forEach(function(post){ %>
    <h3><%= post.title %></h3>

     <!-- .substring method is used to limit post content to 100 character only. -->
    <p ><%= post.blogBody.substring(0,100) + "..." %> <a href="/posts/<%=post.title %>">Read more</a>  </p> 
<% }) %>


<%-include('partials/footer')%>